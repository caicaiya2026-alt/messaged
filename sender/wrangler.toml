name = "message-sender"
main = "index.html"
compatibility_date = "2024-01-01"

# Cloudflare Pages 配置
[site]
bucket = "."

# 环境变量（可选）
[env.production]
name = "message-sender-prod"

[env.staging]
name = "message-sender-staging"
